# README

This directory contains Dockerfiles and artifacts for creating runner and formatter images.

I use some of these images in `snippet-checker` tests,
and also when running `snippet-checker` myself for real.

Anyone who has to make their own runner or formatter images could use these examples.
